# Third-party notices

Runtime and tests use only the Python standard library. The project includes
no vendored third-party code, provider SDK, model weights, external dataset,
or copied benchmark implementation.

The Apache License text is included as `LICENSE`. The Mermaid diagram in the
README is source text, not a bundled third-party binary asset. Future provider
adapters and datasets require an exact upstream, version or commit, license,
and attribution entry before inclusion.
