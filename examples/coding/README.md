# Coding example

Use a precise synthetic function specification with protected identifiers and
run deterministic contract checks. The MVP records a proposal receipt only;
it never applies generated code.
