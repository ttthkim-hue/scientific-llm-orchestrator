# Scorecards

No real-model scorecard is included in the research prototype. Future runs
must preserve the evidence labels and report quality, latency, hosted-token
displacement, local resource use, fallback, privacy, and reproducibility as
separate fields.
